export interface Record {
    id: string;
    record_name: string;
    page_id: string;
    created_at: string;
    record_title: string;
    record_description: string;
    record_code: string;
    category_id: number;
}
